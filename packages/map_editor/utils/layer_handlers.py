from dt_maps.Map import REGISTER

from typing import TypeVar
from layers import BasicLayer

'''for layer in layers_register:
    if layer not in REGISTER:
        # create handler
        REGISTER[layer] = layers_register[layer]
        pass
'''
#vehicle = REGISTER["tiles"]
#print(vehicle.__dict__)